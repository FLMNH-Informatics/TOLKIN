%tr
  %td
    %input{ :type=>'checkbox',  :checked=>'checked, : class=>'cb_filter_view', :id=>"cb_#{option.text.strip().underscore().gsub(' ', '_')}"
  %td
    %label{ :for=>"search_#{option.text.strip().underscore().gsub(' ', '_')}_like"} "#{option.text}"
  %td
    %input{ :type=>'text', :size=>'30',  :name=>"search[#{option.text.strip().underscore().gsub(' ', '_') }_like]",  :id=>"search_#{option.text.strip().underscore().gsub(' ', '_') }_like" }
